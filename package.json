{
  "name": "rocket-moto-base",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd mini-app && npm install && npm run build",
    "dev": "cd mini-app && npm install && npm run dev"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.2.1"
  },
  "devDependencies": {
    "hardhat": "^3.0.7",
    "ethers": "^6.15.0",
    "typescript": "~5.8.0",
    "@types/node": "^22.18.11"
  }
}
